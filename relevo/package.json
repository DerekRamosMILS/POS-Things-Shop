{
  "name": "things-shop-relevo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "tsc --noEmit && vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.6",
    "typescript": "^7.0.2",
    "vitest": "~4.1.11",
    "wrangler": "^4.130.0"
  }
}
